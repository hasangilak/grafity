{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Detect Patterns executor",
  "description": "Detects React patterns and anti-patterns in components",
  "type": "object",
  "properties": {
    "projectRoot": {
      "type": "string",
      "description": "Root directory of the project to analyze"
    },
    "outputPath": {
      "type": "string",
      "description": "Path to save the pattern analysis report"
    },
    "format": {
      "type": "string",
      "enum": ["json", "markdown", "html"],
      "default": "markdown",
      "description": "Output format for the pattern report"
    }
  },
  "required": ["projectRoot", "outputPath"]
}